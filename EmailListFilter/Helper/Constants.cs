using System.Reflection;

namespace EmailListFilter.Helper
{
    public static class Constants
    {
        public const string OutputFilePath = $"../../../../EmailListFilter/Output/FunctionsExecutionTime.txt";
        public const string ConnectionString = "server = 127.0.0.1; user id = root; password = root; database = users; charset = utf8; pooling = false";
    }
}
